{%- if section.blocks.size > 0 -%}
  {%- unless section.settings.show_to_guests_only and customer and request.design_mode == false -%}

{%- comment -%} Banner Section {%- endcomment -%}
<section class="banner-popup-section" id="banner-{{ section.id }}">
  {%- style -%}
    #banner-{{ section.id }} {
      --banner-height: {{ section.settings.banner_height }}px;
      --banner-bg-color: {{ section.settings.banner_bg_color }};
      --banner-text-color: {{ section.settings.banner_text_color }};
      --button-bg-color: {{ section.settings.button_bg_color }};
      --button-text-color: {{ section.settings.button_text_color }};
      --button-font-size: {{ section.settings.button_font_size }}px;
      min-height: var(--banner-height);
      background-color: var(--banner-bg-color);
      color: var(--banner-text-color);
    }

    #banner-{{ section.id }} .banner-popup__container {
      display: grid;
      grid-template-columns: 1fr auto 1fr;
      align-items: center;
      gap: 2rem;
      min-height: var(--banner-height);
      padding: 2rem;
    }

    #banner-{{ section.id }} .banner-popup__action {
      display: flex;
      justify-content: flex-end;
      margin-right: 1rem;
    }

    #banner-{{ section.id }} .banner-popup__button {
      background-color: var(--button-bg-color);
      color: var(--button-text-color);
      font-size: var(--button-font-size);
      border-radius: 50px;
      padding: 1rem 2rem;
      border: none;
      cursor: pointer;
      text-decoration: none;
      display: inline-block;
      transition: all 0.3s ease;
      white-space: nowrap;
    }

    #banner-{{ section.id }} .banner-popup__button:hover {
      opacity: 0.9;
      transform: translateY(-2px);
    }

    {%- if section.settings.banner_bg_image != blank -%}
      #banner-{{ section.id }} {
        background-image: url('{{ section.settings.banner_bg_image | img_url: '1920x' }}');
        background-size: cover;
        background-position: center;
        background-repeat: no-repeat;
      }
    {%- endif -%}

    /* Banner Description Styles */
    #banner-{{ section.id }} .banner-popup__description {
      margin-bottom: 0.5rem;
      line-height: 1.5;
    }
    
    /* Force banner description styles with higher specificity */
    #banner-{{ section.id }} .banner-popup__description,
    #banner-{{ section.id }} .banner-popup__description *,
    #banner-{{ section.id }} .banner-popup__description p,
    #banner-{{ section.id }} .banner-popup__description span,
    #banner-{{ section.id }} .banner-popup__description div {
      font-size: inherit !important;
      font-weight: inherit !important;
      color: inherit !important;
      margin: 0 !important;
      padding: 0 !important;
    }
    
    #banner-{{ section.id }} .banner-popup__description {
      margin-bottom: 0.5rem !important;
    }

    /* Responsive Design */
    @media (max-width: 768px) {
      #banner-{{ section.id }} {
        margin-left: 1rem;
        margin-right: 1rem;
      }
      
      #banner-{{ section.id }} .banner-popup__container {
        grid-template-columns: 1fr;
        text-align: center;
        gap: 1.5rem;
        padding: 1.5rem 1rem;
        justify-items: center;
        align-items: center;
      }
      
      #banner-{{ section.id }} .banner-popup__content {
        display: flex;
        flex-direction: column;
        align-items: center;
        text-align: center;
      }
      
      #banner-{{ section.id }} .banner-popup__timeline {
        justify-content: center;
        align-items: center;
      }
      
      #banner-{{ section.id }} .banner-popup__action {
        justify-content: center;
        margin-right: 0;
      }
      
      #banner-{{ section.id }} .banner-popup__button {
        padding: 0.8rem 1.5rem;
        font-size: calc(var(--button-font-size) * 0.9);
      }
    }

    @media (max-width: 480px) {
      #banner-{{ section.id }} {
        margin-left: 0.5rem;
        margin-right: 0.5rem;
      }
      
      #banner-{{ section.id }} .banner-popup__container {
        padding: 1rem 0.75rem;
        gap: 1rem;
      }
    }
  {%- endstyle -%}

  {%- if section.settings.banner_bg_image != blank -%}
    <div class="banner-popup__bg-overlay" style="background-color: {{ section.settings.banner_bg_color }}; opacity: {{ section.settings.banner_bg_opacity | divided_by: 100.0 }};"></div>
  {%- endif -%}

  <div class="banner-popup__container">
    <!-- Left Content Area -->
    <div class="banner-popup__content">
      {%- for block in section.blocks -%}
        {%- case block.type -%}
          {%- when 'banner_title' -%}
            {%- if block.settings.title != blank -%}
              <h2 class="banner-popup__title" 
                  style="font-size: {{ block.settings.font_size }}px; 
                         font-weight: {{ block.settings.font_weight }}; 
                         color: {{ block.settings.text_color }}; 
                         margin-top: {{ block.settings.margin_top | default: 0 }}px; 
                         margin-bottom: {{ block.settings.margin_bottom | default: 0 }}px;"
                  {{ block.shopify_attributes }}>
                {% if request.locale.iso_code == 'de' %}
                  {{ block.settings.title_de | escape }}
                {% elsif request.locale.iso_code == 'fr' %}
                  {{ block.settings.title_fr | escape }}
                {% elsif request.locale.iso_code == 'fi' %}
                  {{ block.settings.title_fi | escape }}
                {% else %}
                  {{ block.settings.title | escape }}
                {% endif %}
              </h2>
            {%- endif -%}

          {%- when 'banner_description' -%}
            {%- if block.settings.description != blank -%}
              <div class="banner-popup__description" 
                   style="font-size: {{ block.settings.font_size }}px !important; 
                          font-weight: {{ block.settings.font_weight }} !important; 
                          color: {{ block.settings.text_color }} !important; 
                          margin-top: {{ block.settings.margin_top | default: 0 }}px !important; 
                          margin-bottom: {{ block.settings.margin_bottom | default: 0 }}px !important;"
                   {{ block.shopify_attributes }}>
                {% assign description_content = block.settings.description %}
                {% if request.locale.iso_code == 'de' %}
                  {% assign description_content = block.settings.description_de %}
                {% elsif request.locale.iso_code == 'fr' %}
                  {% assign description_content = block.settings.description_fr %}
                {% elsif request.locale.iso_code == 'fi' %}
                  {% assign description_content = block.settings.description_fi %}
                {% endif %}
                {{ description_content }}
              </div>
            {%- endif -%}
        {%- endcase -%}
      {%- endfor -%}
    </div>

    <!-- Middle Timeline/Countdown Area -->
    <div class="banner-popup__timeline">
      {%- for block in section.blocks -%}
        {%- case block.type -%}
          {%- when 'timeline_item' -%}
            <div class="timeline-item" {{ block.shopify_attributes }}>
              <div class="timeline-bullet">•</div>
              <span class="timeline-text">
                {% if request.locale.iso_code == 'de' %}
                  {{ block.settings.timeline_text_de | escape }}
                {% elsif request.locale.iso_code == 'fr' %}
                  {{ block.settings.timeline_text_fr | escape }}
                {% elsif request.locale.iso_code == 'fi' %}
                  {{ block.settings.timeline_text_fi | escape }}
                {% else %}
                  {{ block.settings.timeline_text | escape }}
                {% endif %}
              </span>
            </div>

          {%- when 'banner_countdown' -%}
            <script src="{{ 'countdown-timer.js' | asset_url }}" defer></script>
            <div class="banner-countdown countdown__block countdown-font-{{ block.settings.number_font }} countdown-divider-{{ block.settings.divider_type }} no-js-hidden" id="{{ block.id }}" {{ block.shopify_attributes }}>
              {% render 'countdown-timer',
                end_date: block.settings.end_date,
                end_time: block.settings.end_time,
                end_text: block.settings.end_text,
                end_midnight: true,
                hide_on_end: block.settings.hide_on_end,
                show_labels: block.settings.show_timer_labels
              %}
            </div>
        {%- endcase -%}
      {%- endfor -%}
    </div>

    <!-- Right Button Area -->
    <div class="banner-popup__action">
      {%- liquid
        assign current_button_link = section.settings.button_link_en
        if request.locale.iso_code == 'de' and section.settings.button_link_de != blank
          assign current_button_link = section.settings.button_link_de
        elsif request.locale.iso_code == 'fr' and section.settings.button_link_fr != blank
          assign current_button_link = section.settings.button_link_fr
        elsif request.locale.iso_code == 'fi' and section.settings.button_link_fi != blank
          assign current_button_link = section.settings.button_link_fi
        endif
      -%}
      
      {%- if current_button_link != blank -%}
        <a href="{{ current_button_link }}" 
           class="banner-popup__button"
           {% if section.settings.button_target_blank %}target="_blank" rel="noopener noreferrer"{% endif %}>
          {% if request.locale.iso_code == 'de' %}
            {{ section.settings.button_text_de | escape }}
          {% elsif request.locale.iso_code == 'fr' %}
            {{ section.settings.button_text_fr | escape }}
          {% elsif request.locale.iso_code == 'fi' %}
            {{ section.settings.button_text_fi | escape }}
          {% else %}
            {{ section.settings.button_text | escape }}
          {% endif %}
        </a>
      {%- else -%}
        <button type="button" class="banner-popup__button" disabled style="opacity: 0.5; cursor: not-allowed;">
          {% if request.locale.iso_code == 'de' %}
            {{ section.settings.button_text_de | default: 'Link konfigurieren' | escape }}
          {% elsif request.locale.iso_code == 'fr' %}
            {{ section.settings.button_text_fr | default: 'Configurer le lien' | escape }}
          {% elsif request.locale.iso_code == 'fi' %}
            {{ section.settings.button_text_fi | default: 'Määritä linkki' | escape }}
          {% else %}
            {{ section.settings.button_text | default: 'Configure Link' | escape }}
          {% endif %}
        </button>
      {%- endif -%}
    </div>
  </div>
</section>

<style>
.banner-popup-section {
  position: relative;
  overflow: hidden;
  max-width: {{ section.settings.banner_max_width }}px;
  margin: {{ section.settings.banner_margin_top }}px auto {{ section.settings.banner_margin_bottom }}px auto;
  border-radius: 12px;
}

.banner-popup__bg-overlay {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  pointer-events: none;
}

.banner-popup__timeline {
  display: flex;
  flex-direction: column;
  gap: 0.5rem;
  align-items: center;
}

.timeline-item {
  display: flex;
  align-items: center;
  gap: 0.5rem;
  white-space: nowrap;
}

.timeline-bullet {
  color: #4CAF50;
  font-weight: bold;
  font-size: 1.2em;
}

.timeline-text {
  font-size: 0.9rem;
}

.banner-countdown {
  text-align: center;
}
</style>

  {%- endunless -%}
{%- endif -%}

{% schema %}
{
  "name": "Banner with Custom Link",
  "tag": "section",
  "class": "cc-banner-popup",
  "settings": [
    {
      "type": "header",
      "content": "Banner Settings"
    },
    {
      "type": "range",
      "id": "banner_height",
      "label": "Banner height",
      "min": 200,
      "max": 800,
      "step": 50,
      "unit": "px",
      "default": 400
    },
    {
      "type": "range",
      "id": "banner_max_width",
      "label": "Banner maximum width",
      "min": 1200,
      "max": 2000,
      "step": 50,
      "unit": "px",
      "default": 1600,
      "info": "Maximum width of the banner container"
    },
    {
      "type": "range",
      "id": "banner_margin_top",
      "label": "Banner margin top",
      "min": 0,
      "max": 200,
      "step": 10,
      "unit": "px",
      "default": 60
    },
    {
      "type": "range",
      "id": "banner_margin_bottom",
      "label": "Banner margin bottom",
      "min": 0,
      "max": 200,
      "step": 10,
      "unit": "px",
      "default": 60
    },
    {
      "type": "color",
      "id": "banner_bg_color",
      "label": "Banner background color",
      "default": "#f8f9fa"
    },
    {
      "type": "color",
      "id": "banner_text_color",
      "label": "Banner text color",
      "default": "#333333"
    },
    {
      "type": "image_picker",
      "id": "banner_bg_image",
      "label": "Banner background image"
    },
    {
      "type": "range",
      "id": "banner_bg_opacity",
      "label": "Background overlay opacity",
      "min": 0,
      "max": 100,
      "step": 10,
      "unit": "%",
      "default": 50,
      "info": "Only applies when background image is set"
    },
    {
      "type": "header",
      "content": "Button Settings"
    },
    {
      "type": "text",
      "id": "button_text",
      "label": "Button text",
      "default": "Learn More"
    },
    {
      "type": "text",
      "id": "button_text_de",
      "label": "Button text (German)",
      "default": "Mehr erfahren"
    },
    {
      "type": "text",
      "id": "button_text_fr",
      "label": "Button text (French)",
      "default": "En savoir plus"
    },
    {
      "type": "text",
      "id": "button_text_fi",
      "label": "Button text (Finnish)",
      "default": "Lue lisää"
    },
    {
      "type": "url",
      "id": "button_link_en",
      "label": "Button link (English)",
      "info": "The URL the button should link to for English users (also used as default)"
    },
    {
      "type": "url",
      "id": "button_link_de",
      "label": "Button link (German)",
      "info": "The URL the button should link to for German users"
    },
    {
      "type": "url",
      "id": "button_link_fr",
      "label": "Button link (French)",
      "info": "The URL the button should link to for French users"
    },
    {
      "type": "url",
      "id": "button_link_fi",
      "label": "Button link (Finnish)",
      "info": "The URL the button should link to for Finnish users"
    },
    {
      "type": "checkbox",
      "id": "button_target_blank",
      "label": "Open link in new tab",
      "default": false
    },
    {
      "type": "color",
      "id": "button_bg_color",
      "label": "Button background color",
      "default": "#007bff"
    },
    {
      "type": "color",
      "id": "button_text_color",
      "label": "Button text color",
      "default": "#ffffff"
    },
    {
      "type": "range",
      "id": "button_font_size",
      "label": "Button font size",
      "min": 12,
      "max": 24,
      "step": 1,
      "unit": "px",
      "default": 16
    },
    {
      "type": "header",
      "content": "Visibility Settings"
    },
    {
      "type": "checkbox",
      "id": "show_to_guests_only",
      "label": "Show to guest users only",
      "info": "Not shown to logged in customers.",
      "default": true
    }
  ],
  "blocks": [
    {
      "type": "banner_title",
      "name": "Banner Title",
      "settings": [
        {
          "type": "text",
          "id": "title",
          "label": "Title",
          "default": "Special Offer"
        },
        {
          "type": "text",
          "id": "title_de",
          "label": "Title (German)",
          "default": "Sonderangebot"
        },
        {
          "type": "text",
          "id": "title_fr",
          "label": "Title (French)",
          "default": "Offre spéciale"
        },
        {
          "type": "text",
          "id": "title_fi",
          "label": "Title (Finnish)",
          "default": "Erikoistarjous"
        },
        {
          "type": "range",
          "id": "font_size",
          "label": "Font size",
          "min": 14,
          "max": 48,
          "step": 2,
          "unit": "px",
          "default": 24
        },
        {
          "type": "select",
          "id": "font_weight",
          "label": "Font weight",
          "options": [
            {
              "value": "300",
              "label": "Light"
            },
            {
              "value": "400",
              "label": "Normal"
            },
            {
              "value": "500",
              "label": "Medium"
            },
            {
              "value": "600",
              "label": "Semi Bold"
            },
            {
              "value": "700",
              "label": "Bold"
            },
            {
              "value": "800",
              "label": "Extra Bold"
            }
          ],
          "default": "600"
        },
        {
          "type": "color",
          "id": "text_color",
          "label": "Text color",
          "default": "#333333"
        },
        {
          "type": "range",
          "id": "margin_top",
          "label": "Margin top",
          "min": 0,
          "max": 50,
          "step": 1,
          "unit": "px",
          "default": 0
        },
        {
          "type": "range",
          "id": "margin_bottom",
          "label": "Margin bottom",
          "min": 0,
          "max": 50,
          "step": 1,
          "unit": "px",
          "default": 0
        }
      ]
    },
    {
      "type": "banner_description",
      "name": "Banner Description",
      "settings": [
        {
          "type": "richtext",
          "id": "description",
          "label": "Description",
          "default": "<p>Get amazing deals on our products</p>"
        },
        {
          "type": "richtext",
          "id": "description_de",
          "label": "Description (German)",
          "default": "<p>Erhalten Sie tolle Angebote für unsere Produkte</p>"
        },
        {
          "type": "richtext",
          "id": "description_fr",
          "label": "Description (French)",
          "default": "<p>Obtenez des offres incroyables sur nos produits</p>"
        },
        {
          "type": "richtext",
          "id": "description_fi",
          "label": "Description (Finnish)",
          "default": "<p>Hanki upeita tarjouksia tuotteistamme</p>"
        },
        {
          "type": "range",
          "id": "font_size",
          "label": "Font size",
          "min": 12,
          "max": 24,
          "step": 1,
          "unit": "px",
          "default": 16
        },
        {
          "type": "select",
          "id": "font_weight",
          "label": "Font weight",
          "options": [
            {
              "value": "300",
              "label": "Light"
            },
            {
              "value": "400",
              "label": "Normal"
            },
            {
              "value": "500",
              "label": "Medium"
            },
            {
              "value": "600",
              "label": "Semi Bold"
            },
            {
              "value": "700",
              "label": "Bold"
            }
          ],
          "default": "400"
        },
        {
          "type": "color",
          "id": "text_color",
          "label": "Text color",
          "default": "#666666"
        },
        {
          "type": "range",
          "id": "margin_top",
          "label": "Margin top",
          "min": 0,
          "max": 50,
          "step": 1,
          "unit": "px",
          "default": 0
        },
        {
          "type": "range",
          "id": "margin_bottom",
          "label": "Margin bottom",
          "min": 0,
          "max": 50,
          "step": 1,
          "unit": "px",
          "default": 0
        }
      ]
    },
    {
      "type": "timeline_item",
      "name": "Timeline Item",
      "settings": [
        {
          "type": "text",
          "id": "timeline_text",
          "label": "Timeline text",
          "default": "Oct 23 Teaser"
        },
        {
          "type": "text",
          "id": "timeline_text_de",
          "label": "Timeline text (German)",
          "default": "23. Okt Teaser"
        },
        {
          "type": "text",
          "id": "timeline_text_fr",
          "label": "Timeline text (French)",
          "default": "23 Oct Teaser"
        },
        {
          "type": "text",
          "id": "timeline_text_fi",
          "label": "Timeline text (Finnish)",
          "default": "23. loka Teaser"
        }
      ]
    },
    {
      "type": "banner_countdown",
      "name": "Banner Countdown",
      "limit": 1,
      "settings": [
        {
          "type": "text",
          "id": "end_date",
          "label": "End date",
          "info": "Required. Format: YYYY-MM-DD."
        },
        {
          "type": "text",
          "id": "end_time",
          "label": "End time (optional)",
          "info": "Format: 17:00 (24 hour clock). Uses the store's timezone."
        },
        {
          "type": "richtext",
          "id": "end_text",
          "label": "End text",
          "info": "This shows when the date and time elapses."
        },
        {
          "type": "checkbox",
          "id": "hide_on_end",
          "label": "Hide the timer when the date and time elapses",
          "default": false
        },
        {
          "type": "select",
          "id": "number_font",
          "label": "Number font",
          "options": [
            {
              "label": "Text font",
              "value": "base"
            },
            {
              "label": "Heading font",
              "value": "heading"
            }
          ],
          "default": "base"
        },
        {
          "type": "select",
          "id": "divider_type",
          "label": "Divider type",
          "default": "colon",
          "options": [
            {
              "value": "colon",
              "label": "Colon"
            },
            {
              "value": "dot",
              "label": "Dot"
            },
            {
              "value": "line",
              "label": "Line"
            },
            {
              "value": "none",
              "label": "None"
            }
          ]
        },
        {
          "type": "checkbox",
          "id": "show_timer_labels",
          "label": "Show labels",
          "default": true
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Banner with Custom Link",
      "blocks": [
        {
          "type": "banner_title"
        },
        {
          "type": "banner_description"
        },
        {
          "type": "timeline_item"
        },
        {
          "type": "timeline_item"
        }
      ]
    }
  ],
  "disabled_on": {
    "groups": [
      "header",
      "footer"
    ]
  }
}
{% endschema %}